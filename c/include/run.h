#ifndef _run_h
#define _run_h

#include "cslib.h"

/*
 * Function: newRefInt
 * Usage: ref = newRefInt(value);
 * ------------------------------
 * Allocates a ref pointing to the specified <code>int</code> value.
 */

int run(void);

#endif
